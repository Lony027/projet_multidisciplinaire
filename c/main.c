#include <stdlib.h>

#include "src/matrice.h"

int main() {

    /*
    Matrice m = (Matrice)malloc(sizeof(Matrice));
    initMatrix(m, 10);


    open_csv_matrice("../mock.csv", m);
    printMatrix(m);

    freeMatrix(m);
    */


}
